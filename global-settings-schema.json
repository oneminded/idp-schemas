{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://raw.githubusercontent.com/oneminded/idp-schemas/refs/heads/main/global-settings-schema.json",
  "title": "Global Settings Schema",
  "description": "Global settings for OneMinded IDP Quickstart Toolbox",
  "type": "object",
  "required": [
    "tenantId",
    "uniqueId",
    "serviceShort",
    "serviceDisplayName",
    "regionCode",
    "branchingStrategy"
  ],
  "anyOf": [
    { "required": ["location"] },
    { "required": ["region"] }
  ],
  "properties": {
    "$schema": {
      "description": "Optional schema reference for tooling.",
      "type": "string"
    },
    "tenantId": {
      "description": "The Azure tenant ID (GUID) for the deployment.",
      "type": "string"
    },
    "subscriptionId": {
      "description": "The Azure subscription ID (GUID) for the deployment.",
      "type": "string"
    },
    "uniqueId": {
      "description": "Unique identifier for the deployment",
      "type": "string",
      "pattern": "^[a-z0-9]{6,12}$"
    },
    "serviceShort": {
      "description": "Short service name used in resource names.",
      "type": "string",
      "minLength": 1
    },
    "serviceDisplayName": {
      "description": "Display name for the service",
      "type": "string",
      "minLength": 1
    },
    "location": {
      "description": "Azure location name used for the deployment.",
      "type": "string",
      "minLength": 1,
      "examples": [
        "westeurope",
        "northeurope"
      ]
    },
    "region": {
      "description": "Azure region name used for the deployment (alias for location).",
      "type": "string",
      "minLength": 1,
      "examples": [
        "westeurope",
        "northeurope"
      ]
    },
    "regionCode": {
      "description": "Azure region code used in names.",
      "type": "string",
      "minLength": 1,
      "maxLength": 5,
      "examples": [
        "weu",
        "neu"
      ]
    },
    "branchingStrategy": {
      "description": "Repository branching strategy.",
      "type": "string",
      "enum": [
        "github",
        "release"
      ]
    }
  },
  "additionalProperties": false
}
